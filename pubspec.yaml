name: analyn_admin
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  # --- Firebase Core ---
  firebase_core: ^4.2.1        # Updated to a stable, null-safe version
  firebase_auth: ^6.1.2        # Updated
  cloud_firestore: ^6.1.0      # Updated
  firebase_storage: ^13.0.4    # Updated
  firebase_messaging: ^16.0.4  # Updated
  firebase_app_check: ^0.4.1+2 # Necessary for security, ensure this is present
  
  # --- UI & Utilities ---
  google_fonts: ^6.2.1
  provider: ^6.1.2
  file_picker: ^10.3.7         # Updated
  cupertino_icons: ^1.0.8
  
  # --- Services & Plugins ---
  cloud_functions: ^6.0.4
  url_launcher: ^6.3.1         # Use the top-level package
  geolocator: ^14.0.2          # Updated
  flutter_blue_plus: ^2.0.2    # Updated

# Remove the dependency_overrides section if you added it earlier.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
